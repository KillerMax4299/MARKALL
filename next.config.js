/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    use: true,
  },
  images: {
    unoptimized: true,
  },
  // reactStrictMode: false,
};

module.exports = nextConfig
